

const getAllModels = () => {
  console.log("Getting all models...")
};

const addModel = () => {
  console.log("Add your model...")
};

const editModelByID = () => {
  console.log("Getting that model by their ID...")
};

const deleteModelByID = () => {
  console.log("Deleting that model by their ID,,,")
};


export {
  getAllModels,
  addModel,
  editModelByID,
  deleteModelByID
};